# Talks

This repo contains slides for some of my talks. I probably won't bother converting older slides to this new setup (but then again, I might).

Check out the `build` branch for "compiled" slides (pdf, ppt, html).
